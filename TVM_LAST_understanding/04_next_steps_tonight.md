# 今晚执行任务清单（按优先级）

## P0（必须完成，先做）

1. 建立固定基线（20 分钟）  
完成标准：明确并写入 `target`、shape 桶、线程配置、baseline 延迟。

2. 跑通 quick 模式（30-40 分钟）  
完成标准：产出 1 份 quick 报告，包含 baseline/current、方差信息、DB 路径。

3. 锁定热点任务（15 分钟）  
完成标准：拿到前 `3-8` 个热点 task 列表，并作为 full 任务唯一输入。

## P1（今晚建议完成）

1. 提交 full 夜跑任务（15 分钟）  
完成标准：full 任务已启动，日志写入 `session_bootstrap/logs/`，输出写入 `session_bootstrap/reports/`。

2. 记录单变量实验（10 分钟）  
完成标准：在实验记录模板中仅记录一个改动变量和预期收益。

3. 校验 DB 可复用（10 分钟）  
完成标准：第二次 quick 能加载首轮 DB，执行时间或效果有可解释变化。

## P2（有余力再做）

1. 为失败样本建立最小复现条目。  
2. 统计 quick 与 full 的单位时间收益。  
3. 给明天第一轮实验写好候选参数范围。

## 今晚执行顺序（建议）

1. 先完成 P0 三项。  
2. 再完成 P1 三项。  
3. P2 只在时间充足时执行。

## 明早交接输出（最少）

```text
- baseline 与 current 对比（同条件）
- full 夜跑是否完成（是/否 + 原因）
- 最佳候选来自哪个 task
- 是否存在不稳定项（及定位方向）
- 今日唯一改动变量与明日计划
```
