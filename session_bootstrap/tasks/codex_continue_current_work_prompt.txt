继续当前工作。

先检查 `/home/tianxing/tvm_metaschedule_execution_project/PLAN_DEGRADATION_UPPER_COMPUTER.md` 里的方案目前是否已经全部执行完成。优先根据当前会话、仓库现状、已有实现和现有接口判断，不要重复做已经完成的工作。

执行目标：
- 如果该方案还没有执行完，就严格按照 `PLAN_DEGRADATION_UPPER_COMPUTER.md` 继续推进，优先延续当前工作和最近上下文，不要偏航到无关 side quest。
- 如果该方案已经执行完成，就不要做任何事情，只回复“好的，master”。

关键提醒：
- 当前环境就是上位机，也就是笔记本；默认你现在就在上位机环境里工作。
- 如果需要连接下位机/飞腾派，不要新写连接逻辑，不要自己猜连接方式，只调用仓库里已经存在的 SSH 脚本。
- 优先使用 `session_bootstrap/scripts/connect_phytium_pi.sh`；如有必要，再使用同目录下现有的 SSH 辅助脚本。
- 以 `PLAN_DEGRADATION_UPPER_COMPUTER.md` 的约束为准推进实现。
