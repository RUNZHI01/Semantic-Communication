继续当前工作。

先检查下面这些任务目前是否已经完成，优先根据当前会话、仓库现状和现有接口判断，不要重复做已经完成的工作：

1. 推进 electron demo 的兼容性、移植性修复。
2. 在板卡上接入真正的定位 API 服务，并将定位信息接入我目前的 demo 程序中；目前我们已有接口。
3. 修复 demo 中对于板卡系统占用情况的监控，尤其是内存占用监控。

执行要求：
- 如果以上任务都已经完成，只回复“好的master”，不要做任何事情。
- 如果还有未完成项，按上面的顺序依次执行。
- 优先延续当前工作和最近上下文，不要偏航到无关 side quest。
