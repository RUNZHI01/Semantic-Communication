# RPC Command Templates

- generated_at: 2026-03-01T13:18:06+08:00
- env_file: /home/tianxing/tvm_metaschedule_execution_project/./session_bootstrap/reports/rpc_run_env_20260301_131806.env

## 1) Tracker（开发机，builder/orchestrator 侧）

```bash
python3 -m tvm.exec.rpc_tracker --host "0.0.0.0" --port "9190"
```

## 2) RPC Server（ARMv8 真机，runner 侧）

```bash
python3 -m tvm.exec.rpc_server --tracker "127.0.0.1:9190" --key "armv8_smoke" --host "0.0.0.0" --port "9090" --port-end "9099"
```

## 3) Client（开发机，quick/full 触发）

```bash
bash "/home/tianxing/tvm_metaschedule_execution_project/session_bootstrap/scripts/run_quick.sh" --env "/home/tianxing/tvm_metaschedule_execution_project/./session_bootstrap/reports/rpc_run_env_20260301_131806.env"
bash "/home/tianxing/tvm_metaschedule_execution_project/session_bootstrap/scripts/run_full_placeholder.sh" --env "/home/tianxing/tvm_metaschedule_execution_project/./session_bootstrap/reports/rpc_run_env_20260301_131806.env"
```

## 4) Client（一键首轮闭环入口）

```bash
bash "/home/tianxing/tvm_metaschedule_execution_project/session_bootstrap/scripts/run_rpc_first_round.sh" --env "/home/tianxing/tvm_metaschedule_execution_project/./session_bootstrap/reports/rpc_run_env_20260301_131806.env"
```

## 5) 当前 env 中的 quick/full payload 命令

```bash
# quick
mkdir -p "$TUNING_DB_DIR/quick" && echo "baseline $(date -Iseconds)" >> "$TUNING_DB_DIR/quick/history.log" && sleep 0.8
mkdir -p "$TUNING_DB_DIR/quick" && echo "current $(date -Iseconds)" >> "$TUNING_DB_DIR/quick/history.log" && sleep 0.4

# full
bash "$FULL_RUNNER_SCRIPT" --label baseline --hotspots "$FULL_HOTSPOT_TASKS" --trials-per-task "$FULL_TRIALS_PER_TASK" --work-units "$FULL_BASELINE_WORK_UNITS" --db-dir "$TUNING_DB_DIR/full_hotspot_runs"
bash "$FULL_RUNNER_SCRIPT" --label current --hotspots "$FULL_HOTSPOT_TASKS" --trials-per-task "$FULL_TRIALS_PER_TASK" --work-units "$FULL_CURRENT_WORK_UNITS" --db-dir "$TUNING_DB_DIR/full_hotspot_runs"
```
