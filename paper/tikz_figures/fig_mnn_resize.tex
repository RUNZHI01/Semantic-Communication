% 图 4.7 resizeTensor 和 resizeSession 示例 — 基于 CICC0903540 技术文档 4.4.1
\documentclass[tikz,border=8pt]{standalone}
\input{_preamble}
\begin{document}
\begin{tikzpicture}[
  x=1.1cm, y=1.0cm,
  node distance=0.45cm and 0.6cm,
  code/.style={
    rectangle, draw=mDark, fill=mGray!50,
    minimum width=3.4cm, minimum height=0.55cm,
    font=\scriptsize\ttfamily, align=left, rounded corners=2pt,
    semithick, inner sep=4pt},
  steplbl/.style={font=\scriptsize\sffamily, color=mDark},
  arr/.style={-{Stealth[length=4pt]}, semithick, draw=mDark},
]
  \node[steplbl] (s1) {1. 获取输入};
  \node[code, below=0.12cm of s1] (c1) {input = getSessionInput(session)};

  \node[steplbl, below=0.55cm of c1] (s2) {2. 更新维度};
  \node[code, below=0.12cm of s2] (c2) {resizeTensor(input, [1,C,H,W])};

  \node[steplbl, below=0.55cm of c2] (s3) {3. 重分配内存};
  \node[code, below=0.12cm of s3] (c3) {resizeSession(session)};

  \node[steplbl, below=0.55cm of c3] (s4) {4. 执行推理};
  \node[code, below=0.12cm of s4] (c4) {runSession(session)};

  \draw[arr] (c1) -- (c2);
  \draw[arr] (c2) -- (c3);
  \draw[arr] (c3) -- (c4);

  \node[right=1.8cm of c2, font=\scriptsize, color=mGray, align=left, text width=3.4cm]
    (note) {H, W 为动态轴\\不同图片可设不同尺寸};
  \draw[arr, dashed, color=mGray!80] (c2.east) -- (note.west);

  % 图例（预留边距，避免与 note 重叠）
  \node[legendbox, anchor=north east] at ([xshift=-5pt, yshift=-5pt]current bounding box.south east)
    [align=left, text width=2.8cm] {
    \textbf{图例}\\[1pt]
    Session API 动态形状
  };
\end{tikzpicture}
\end{document}
